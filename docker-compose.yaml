services:

  bot:
    restart: always
    environment:
      - PIP_DEFAULT_TIMEOUT=100
      - PIP_DISABLE_PIP_VERSION_CHECK=1
#      - PIP_NO_CACHE_DIR=1
#      - PYTHONUNBUFFERED=1
#      - POETRY_VERSION=1.0.5

    build:
      dockerfile: bot.dockerfile
      context: .
      target: dev
    volumes:
      - /Users/arsikurin/PycharmProjects/letovoAnalytics:/app-data
#    depends_on:
#      - db

#  api:
#    restart: always
#    ports:
#      - 8081:80
#    build:
#      dockerfile: api.dockerfile
#      context: .

#  db:
#    image: postgres
#    restart: always
#    ports:
#      - 5432:5432
#    volumes:
#      - db-data:/var/lib/postgresql/data

#volumes:
#  db-data:

#    volumes:
#      - type: bind
#        source: .
#        target: /app
#        volume:
#          nocopy: true